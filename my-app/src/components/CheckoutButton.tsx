"use client";
import React from "react";

const CheckoutButton = () => {
  return (
    <div>
      <button className="btn btn-success" onClick={() => alert("da")}>
        Checkout
      </button>
    </div>
  );
};

export default CheckoutButton;
